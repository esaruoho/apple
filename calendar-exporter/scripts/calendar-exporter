#!/usr/bin/env bash
# calendar-exporter — Apple Calendar via SQLite + AppleScript
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/calendar_exporter.py" "$@"
