#!/usr/bin/env bash
# finder-exporter — tags / sidebar / recents
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/finder_exporter.py" "$@"
