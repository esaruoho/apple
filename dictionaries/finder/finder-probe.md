# Finder — Automation Probe

> **9 automation layers** extracted from `/System/Library/CoreServices/Finder.app`
> Generated by `app-probe.py`

## 1. Scripting Dictionary (sdef)

**25 commands**, **32 classes**, **9 suites**

See [`finder.md`](finder.md) for full reference and [`finder-examples.md`](finder-examples.md) for snippets.

Commands: `open`, `print`, `quit`, `activate`, `close`, `count`, `data size`, `delete`, `duplicate`, `exists`, `make`, `move`, `select`, `openVirtualLocation`, `copy`, `sort`, `clean up`, `eject`, `empty`, `erase`, `reveal`, `update`, `restart`, `shut down`, `sleep`

## 2. URL Schemes

| Scheme | Name | Default |
|--------|------|---------|
| `afp://` | AFP URL | Yes |
| `file://` | File URL | Yes |
| `ftp://` | FTP URL | Yes |
| `smb://` | SMB URL | Yes |
| `cifs://` | SMB URL | Yes |
| `nwnode://` | NWNode URL | Yes |

```bash
# Open via URL scheme
open "afp://"
```

## 3. Document Types (11 types)

- **Folder** (Editor)
- **Predefined Search** — `com.apple.finder.predefined-search` (Editor)
- **Saved Search Query** — `.savedSearch` (Editor)
- **AppleTalk Internet Location** — `.atloc` (Editor)
- **NSL Internet Location** — `.nslloc` (Editor)
- **Volume** (Editor)
- **Burn Folder** — `.fpbf` (Editor)
- **Picture Clipping** — `.pictClipping` (Editor)
- **Text Clipping** — `.textClipping` (Editor)
- **Sound Clipping** — `.sndClipping` (Editor)
- **Clipping** — `.Clipping` (Editor)

## 4. App Intents / Siri Phrases

### Siri Phrases

- *"Create a folder named ${fileName} inside ${target}"*
- *"Make a new folder called ${fileName} in ${target}"*
- *"Put a new folder named ${fileName} under ${target}"*
- *"Get info for ${targets}"*
- *"Show me info about ${targets}"*
- *"Bring up the Get Info window for ${targets}"*
- *"Get the currently selected ${applicationName} items"*
- *"Take the current selection"*
- *"Pick what I chose in ${applicationName}"*
- *"Reveal ${targets} in ${applicationName}"*
- *"Open a ${applicationName} window showing ${targets}"*
- *"Show me ${targets} in a ${applicationName} window"*
- *"Rename ${target} to ${newName}"*
- *"Change the name of ${target} to ${newName}"*
- *"Get the ${location}"*
- *"Give me the ${location} folder"*
- *"Go to ${location}"*
- *"Open a ${applicationName} window showing ${location}"*
- *"Show me my ${location}"*
- *"Show the Go To Folder window"*
- *"Go to folder"*
- *"Search for ${criteria} in ${applicationName}"*
- *"Search for ${criteria} in the ${applicationName} app"*
- *"Look for ${criteria} in ${applicationName}"*
- *"Query ${criteria} in my ${applicationName}"*
- *"Search in ${applicationName}"*
- *"Go to the enclosing folder"*
- *"Navigate front ${applicationName} window to its parent folder"*
- *"Show the folder above this one"*
- *"Open ${target}"*
- *"Double click on ${target} in ${applicationName}"*
- *"Open the ${target} folder"*
- *"Open the ${target} file"*
- *"Copy ${sources} to ${destination}"*
- *"Copy ${sources} and move to ${destination}"*
- *"Copy ${sources} and paste in ${destination}"*
- *"Put a copy of ${sources} in ${destination}"*
- *"Copy ${sources} to ${destination} keeping any existing copy"*
- *"Copy ${sources} to ${destination} overwriting conflicting names"*
- *"Copy ${sources} and move to ${destination} replacing the existing copy"*
- *"Copy ${sources} and paste in ${destination} removing anything with the same name"*
- *"Duplicate ${sources}"*
- *"Make a duplicate copy of ${sources}"*
- *"Create another copy of ${sources}"*
- *"Move ${entities} into ${destinationFolder}"*
- *"Put ${entities} in ${destinationFolder}"*
- *"Place ${entities} in the ${destinationFolder} folder"*
- *"Move ${entities} into ${destinationFolder}"*
- *"Put ${entities} in ${destinationFolder}"*
- *"Place ${entities} in the ${destinationFolder} folder"*
- *"Move ${entities} into ${destinationFolder} and keep existing items with the same names"*
- *"Move ${entities} into ${destinationFolder} overwriting conflicting names"*
- *"Put ${entities} in ${destinationFolder} replacing the existing copy"*
- *"Place ${entities} in the ${destinationFolder} folder removing anything with the same name"*
- *"Move ${entities} to the Trash"*
- *"Put ${entities} in the Trash"*
- *"Trash ${entities}"*
- *"Delete my ${entities} files"*
- *"Compress ${sources}"*
- *"Make an archive containing ${sources}"*
- *"Zip the items called ${sources}"*
- *"Zip ${sources}"*
- *"Archive ${sources}"*
- *"Compress ${sources} as ${format}"*
- *"Archive ${sources} as ${format}"*
- *"Compress ${sources} as ${format} into ${destination}"*
- *"Archive ${sources} and place it in ${destination}"*

### Shortcuts Actions (16)

| Action | Title | Parameters |
|--------|-------|------------|
| `GetSelectedItemsIntent` | Get Selected Items |  |
| `GoToLocationIntent` | Go To Location |  |
| `TrashItemsIntent` | Trash Items |  |
| `SearchInBrowserIntent` | Search in Finder |  |
| `GoToFolderIntent` | Go To Folder |  |
| `CompressItemsIntent` | Compress Items |  |
| `MoveItemsIntent` |  |  |
| `CopyItemsIntent` | Copy and Move Items |  |
| `RevealItemsIntent` | Reveal Items |  |
| `GoToEnclosingFolderIntent` | Go To Enclosing Folder |  |
| `GetLocationIntent` | Get Location |  |
| `GetInfoIntent` | Get Info |  |
| `OpenItemIntent` |  |  |
| `RenameItemIntent` |  |  |
| `DuplicateItemsIntent` | Duplicate Items |  |
| `CreateFolderIntent` | New Folder |  |

### Queries (1)

- `FINodeEntityQuery` — 

## 5. Services Menu

- **Finder/Open** → `open`
  - Accepts: `NSStringPboardType`, `NSFilenamesPboardType`, `NSURLPboardType`, `NSMultipleTextSelectionPboardType`
- **Finder/Reveal** → `reveal`
  - Accepts: `NSStringPboardType`, `NSFilenamesPboardType`, `NSURLPboardType`, `NSMultipleTextSelectionPboardType`
- **Finder/Show Info** → `showInfo`
  - Accepts: `NSStringPboardType`, `NSFilenamesPboardType`, `NSURLPboardType`, `NSMultipleTextSelectionPboardType`

## 6. User Activity Types (Handoff/Spotlight)

- `com.apple.clouddocs.viewing`

## 7. Entitlements (88 total)

### Sandbox (6)

- `com.apple.private.security.storage.mobilesync`
- `com.apple.private.security.user-intent-authority`
- `com.apple.security.personal-information.addressbook`
- `com.apple.security.temporary-exception.iokit-user-client-class`
- `com.apple.security.temporary-exception.mach-lookup.global-name`
- `com.apple.security.temporary-exception.shared-preference.read-only`

### Network (1)

- `com.apple.private.network.socket-delegate`

### Icloud (2)

- `com.apple.developer.ubiquity-kvstore-identifier`
- `com.apple.private.appleaccount.app-hidden-from-icloud-settings`

### Privacy (5)

- `com.apple.private.tcc.allow`
- `com.apple.private.tcc.manager.access.delete`
- `com.apple.private.tcc.manager.access.modify`
- `com.apple.private.tcc.manager.access.read`
- `com.apple.private.tcc.manager.check-by-audit-token`

### Other (74)

- `com.apple.accounts.appleaccount.fullaccess`
- `com.apple.amp.devices.client`
- `com.apple.application-identifier`
- `com.apple.authkit.client.private`
- `com.apple.bluetooth.system`
- `com.apple.dock.add-item`
- `com.apple.fileprovider.acl-read`
- `com.apple.fileprovider.enumerate`
- `com.apple.fileprovider.extension-host`
- `com.apple.fileprovider.fetch-url`
- *...and 64 more*

## 8. Linked Frameworks (83 total)

**Automation-relevant:**

- `SafariServices`
- `CloudKit`
- `CoreMedia`
- `AVFoundation`
- `Contacts`
- `AppIntents`

## 11. Plugin Extensions (1)

- **FileSearch** — `com.apple.intents-service`
