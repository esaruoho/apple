# Bluetooth File Exchange — AppleScript Reference

> Extracted from scripting dictionary via `sdef`
> 2 commands, 0 classes, 1 suites

```applescript
tell application "Bluetooth File Exchange"
    -- commands go here
end tell
```

## Bluetooth File Exchange Suite

> The suite of commands for Bluetooth File Exchange

### Commands

#### `browse`

Browse a device.

- **device**: `text` *(optional)* — The device to browse.

#### `send`

Send a file to a bluetooth device.

- **file**: `file` *(optional)* — The file to send.
- **to device**: `text` *(optional)* — The device to send the file to.
