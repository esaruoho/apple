#!/usr/bin/env bash
# stickies-exporter — read-only catalog + markdown vault for Apple Stickies
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/stickies_exporter.py" "$@"
