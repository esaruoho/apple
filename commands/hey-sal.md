---
description: Natural-language router across the Apple bulk-exporter family (mail/safari/notes/voice memos/calendar/photos). Usage `/hey-sal "<sentence>"`.
allowed-tools: Bash
argument-hint: "<natural language sentence>"
---

Route a natural-language query to the right exporter via rule-based intent matching. Apple-native, offline.

Use Bash to execute (one call, then stop):

```
/Users/esaruoho/work/apple/bin/hey-sal $ARGUMENTS
```

After the command completes, report the result verbatim.
