---
description: Warm the on-device embedding cache for a folder, visibly (live per-file progress). Shares vault-grep's cache, so one warm-up makes vault-grep / vault-cluster / vault-consilience fast. Apple-native NLEmbedding, no deps. Use `--status` to check warmth, `--stat` for global cache stats.
---

Warm a corpus for semantic search. Live progress per file. Deterministic, no LLM in the loop.

```bash
/Users/esaruoho/work/apple/bin/embed $ARGUMENTS
```
