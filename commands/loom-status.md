---
description: Show loom-mirror progress on CloudcityMacMini (4TB drive). Deterministic, no LLM analysis. Usage `/loom-status`.
allowed-tools: Bash
argument-hint: (no arguments)
---

Report current state of `bin/loom-mirror` running on CloudcityMacMini.local. Zero roundtrip.

Use Bash to execute (one call, then stop):

```
/Users/esaruoho/work/apple/bin/loom-status
```

After the command completes, report the output verbatim. Do not summarize, re-interpret, or estimate completion times.
