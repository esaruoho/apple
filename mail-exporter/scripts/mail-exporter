#!/usr/bin/env bash
# mail-exporter — Apple Mail metadata via Envelope Index SQLite
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/mail_exporter.py" "$@"
