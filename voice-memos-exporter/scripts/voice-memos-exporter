#!/usr/bin/env bash
# voice-memos-exporter — Apple Voice Memos read-only catalog + audio export
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/voice_memos_exporter.py" "$@"
