#!/usr/bin/env bash
# reminders-exporter — CLI wrapper for Apple Reminders → Markdown vault
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/reminders_exporter.py" "$@"
