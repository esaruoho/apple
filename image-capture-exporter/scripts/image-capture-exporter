#!/usr/bin/env bash
# image-capture-exporter — Image Capture surface via AVFoundation + system_profiler
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/image_capture_exporter.py" "$@"
