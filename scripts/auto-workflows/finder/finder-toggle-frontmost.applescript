-- Toggle frontmost
-- App: Finder
-- Usage: osascript scripts/auto-workflows/finder/finder-toggle-frontmost.applescript
-- Auto-generated by auto-gen.py
-- Concept: tell application ... end tell
--   Sends Apple Events to an app. The app must have a scripting dictionary (sdef).
-- Concept: display notification "text" with title "title"
--   Shows a macOS notification banner. Disappears after a few seconds.

tell application "Finder"
    set frontmost to not frontmost
    if frontmost then
        display notification "frontmost: ON" with title "Finder"
    else
        display notification "frontmost: OFF" with title "Finder"
    end if
end tell
