-- Set rating via dialog prompt
-- App: Music
-- Usage: osascript scripts/auto-workflows/music/music-set-rating.applescript
-- Auto-generated by auto-gen.py
-- Concept: tell application ... end tell
--   Sends Apple Events to an app. The app must have a scripting dictionary (sdef).
-- Concept: display dialog "text" default answer "text"
--   Shows a dialog with a text input field. Returns user input.

tell application "Music"
    set currentVal to rating of current track
    set userInput to display dialog "Set rating:" default answer (currentVal as text) with title "Music" buttons {"Cancel", "OK"} default button "OK"
    set newVal to (text returned of userInput) as number
    set rating of current track to newVal
    display notification "rating: " & newVal with title "Music"
end tell
