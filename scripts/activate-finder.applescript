-- Activate Finder.app — bring it to the foreground
-- Usage: osascript activate-finder.applescript
-- Loupedeck Live: set custom action to run this script

tell application "Finder"
	activate
end tell
