#!/usr/bin/env bash
# audio-midi-exporter — Audio MIDI Setup via system_profiler back-door
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/audio_midi_exporter.py" "$@"
