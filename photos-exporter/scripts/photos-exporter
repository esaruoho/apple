#!/usr/bin/env bash
# photos-exporter — Apple Photos library catalog via Photos.sqlite
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/photos_exporter.py" "$@"
