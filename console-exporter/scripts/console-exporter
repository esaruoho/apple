#!/usr/bin/env bash
# console-exporter — Console.app surface via the `log` CLI back-door
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/console_exporter.py" "$@"
