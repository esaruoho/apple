# Paths secret-scan does not scan. Keep this list SHORT — every entry is a place a
# real secret could hide unseen.
#
# These two DEFINE the patterns, so they necessarily contain strings shaped like the
# things being matched (regex literals, and the masked-digit example in --help).
bin/recburn-redact
bin/secret-scan
# Sal archive: third-party transcripts and scraped scripts, full of hashes and numbers
# that look like account digits. Nothing of Esa's is authored here.
sources/sal/*
indexes/sal-scripts.yaml
