# HomePod climate (pointer)

The watcher + dashboard moved out of this directory. **Canonical home:** `git@github.com:esaruoho/homepod-watcher.git` (private), checked out at `~/work/homepod-watcher/` on CloudcityMacMini and (optionally) on this laptop.

Live data: `~/work/comms/queue/homepod-climate/` (via Syncthing — same path on both Macs). `climate-logs/` here is a symlink into that share so AppleToolbox and the dashboard find it.

See:

- `~/work/apple/wiki/entities/homepod.md` — what the skill knows, reusable patterns
- `~/.claude/skills/homepod/skill.md` — full skill (Syncthing layout, file:// dashboard, locale trap)
- `~/work/homepod-watcher/README.md` — writer details + repo-side history
