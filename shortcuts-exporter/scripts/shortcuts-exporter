#!/usr/bin/env bash
# shortcuts-exporter — Shortcuts.app library catalog via SQLite + shortcuts CLI
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"
exec "$PYTHON" "$SCRIPT_DIR/shortcuts_exporter.py" "$@"
